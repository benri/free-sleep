# Contributing

This is a personal fork of [free-sleep](https://github.com/throwaway31265/free-sleep).

## Development

- All contributions must be in TypeScript
- Run `npm run lint` and `npm run build` in both `server/` and `app/` before opening a PR
- Keep PRs small and focused
