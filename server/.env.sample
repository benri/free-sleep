# Pod defaults — copy to .env.pod (pod) or .env.local (local dev) and fill in secrets
ENV="prod"
DATA_FOLDER="/persistent/free-sleep-data/"
DATABASE_URL="file:/persistent/free-sleep-data/free-sleep.db"
JWT_SECRET=
SERVICE_TOKEN=

# For local development, copy this file to .env.local and adjust:
#   ENV="local"
#   DATA_FOLDER="/path/to/your/free-sleep-data/"
#   DATABASE_URL="file:/path/to/your/free-sleep-data/free-sleep.db"
#   JWT_SECRET=dev-secret
